program calculator
	declare choice, x, y;
	
	function add(in x, in y)
	{
		return (x + y);
	}
	#main program!!!#
	{
		input(choice);
		input(x);
		input(y);
		if (choice = 1)
		{
			print(add(in x, in y));
		}
		
	}.