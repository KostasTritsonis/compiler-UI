program awesome
    declare x, y, z;
    declare w;

    function f1(in a)

        declare z, i;

        procedure f2()
            declare count;

            function f3(inout b,in a)
            {
                return(a + x + z);
            }
        
        {
            call f2();
        }

    {
        if (x<=(w+x))return(x);
        else return(w);
    }

    {

        x := f1(in x);
        print(x);
    }.

       